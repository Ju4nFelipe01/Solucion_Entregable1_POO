package ejercicio5;

public class Calculos {
    static double perimetro_circuferencia(double rario){
        return (2*Math.PI*rario);
    }
    static double area_circulo(double radio){
        return (Math.pow(radio, 2)* Math.PI);
    }
}
